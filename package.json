{
  "name": "flux-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Build handled by vercel.json buildCommand'",
    "start": "echo 'Start not needed for Vercel deployment'"
  }
}
